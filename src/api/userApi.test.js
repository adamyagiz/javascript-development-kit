import {expect} from 'chai'; // using the "expect" style that comes with Chai

// dummy test
describe('userApi testing in progress', () => {
	it('should pass', () => {
		expect(true).to.equal(true);
	});
});
